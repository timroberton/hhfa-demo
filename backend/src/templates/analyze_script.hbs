options(warn=-1)

library(dplyr)
library(haven)
library(readr)
library(ggplot2)

df <- read_dta('DATA_FILE_TO_ANALYZE.dta')

#################################
########## Do analyses ##########
#################################
{{#each analyses}}
#################### START Analysis {{analysisId}} ####################
    {{#each itemIds}}
p <- qplot(data=df, {{this}})
ggsave('{{../analysisId}}_{{this}}_plot.png', plot = p, width = 10, height = 6, dpi = 300)
    {{/each}}
##################### END Analysis {{analysisId}} #####################
{{/each}}
#################################
#################################
#################################

write_csv(df, 'final.csv')